import { n as WatermarkPureOptions, t as WatermarkPluginFrontmatter } from "../options-B6ZA7la5.js";
import { Page } from "vuepress";
import { Plugin } from "vuepress/core";

//#region src/node/options.d.ts
interface WatermarkPluginOptions {
  /**
   * Specify which pages need to have watermarks added.
   *
   * Pages with `true` value will have watermarks added.
   *
   * 指定哪些页面需要添加水印。
   *
   * 拥有 `true` 值的页面将会被添加水印。
   *
   * @default true
   */
  enabled?: boolean | ((page: Page) => boolean);
  /**
   * Watermark options
   *
   * 水印配置选项
   *
   * @see [watermark-js-plus](https://zhensherlock.github.io/watermark-js-plus/config/)
   */
  watermarkOptions?: WatermarkPureOptions;
}
//#endregion
//#region src/node/watermarkPlugin.d.ts
/**
 * VuePress watermark plugin
 *
 * VuePress 水印插件
 *
 * @example
 * ```ts
 * import { watermarkPlugin } from '@vuepress/plugin-watermark'
 *
 * export default {
 *   plugins: [
 *     watermarkPlugin({
 *       enabled: true,
 *       watermarkOptions: {
 *         content: 'My Site',
 *         fontColor: '#666',
 *         opacity: 0.3,
 *       },
 *     }),
 *   ],
 * }
 * ```
 */
declare const watermarkPlugin: ({
  enabled,
  ...options
}?: WatermarkPluginOptions) => Plugin;
//#endregion
export { WatermarkPluginFrontmatter, WatermarkPluginOptions, WatermarkPureOptions, watermarkPlugin };
//# sourceMappingURL=index.d.ts.map