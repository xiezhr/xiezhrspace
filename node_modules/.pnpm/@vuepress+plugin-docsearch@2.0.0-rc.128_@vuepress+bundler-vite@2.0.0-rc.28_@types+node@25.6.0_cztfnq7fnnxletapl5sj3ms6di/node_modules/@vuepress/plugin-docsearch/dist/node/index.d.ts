import { n as DocSearchOptions, r as DocSearchLocaleData, t as DocSearchLocaleOptions } from "../options-CS-U9JQC.js";
import { Plugin } from "vuepress/core";

//#region src/node/options.d.ts
/**
 * Options for `@vuepress/plugin-docsearch`
 *
 * `@vuepress/plugin-docsearch` 的配置项
 */
interface DocSearchPluginOptions extends DocSearchOptions {
  /**
   * Base path of the search index
   *
   * 搜索索引的基础路径
   *
   * @default app.siteData.base
   */
  indexBase?: string;
  /**
   * Whether to inject docsearch default styles
   *
   * 是否注入 docsearch 的默认样式
   *
   * @default true
   */
  injectStyles?: boolean;
}
//#endregion
//#region src/node/docsearchPlugin.d.ts
/**
 * DocSearch plugin
 *
 * DocSearch 插件
 *
 * @example
 * ```ts
 * import { docsearchPlugin } from '@vuepress/plugin-docsearch'
 *
 * export default {
 *   plugins: [
 *     docsearchPlugin({
 *       appId: 'XXXXXX',
 *       apiKey: 'XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX',
 *       indices: ['xxxxxxxx'],
 *       locales: {
 *         '/': {
 *           placeholder: 'Search Documentation',
 *         },
 *         '/zh/': {
 *           placeholder: '搜索文档',
 *         },
 *       },
 *     }),
 *   ],
 * }
 * ```
 *
 * @returns Plugin object / 插件对象
 */
declare const docsearchPlugin: ({
  injectStyles,
  indexBase,
  locales,
  ...options
}?: DocSearchPluginOptions) => Plugin;
//#endregion
export { DocSearchLocaleData, DocSearchLocaleOptions, DocSearchOptions, DocSearchPluginOptions, docsearchPlugin };
//# sourceMappingURL=index.d.ts.map