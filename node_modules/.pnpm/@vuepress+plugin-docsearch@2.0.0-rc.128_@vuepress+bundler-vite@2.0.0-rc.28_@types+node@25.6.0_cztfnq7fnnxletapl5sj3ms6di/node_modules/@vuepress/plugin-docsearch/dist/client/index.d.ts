import { n as DocSearchOptions, r as DocSearchLocaleData, t as DocSearchLocaleOptions } from "../options-CS-U9JQC.js";
import * as _$vue from "vue";
import { MaybeRefOrGetter, PropType, VNode } from "vue";
import { DocSearchProps } from "@docsearch/js";

//#region src/client/components/DocSearch.d.ts
declare const DocSearch: _$vue.DefineComponent<_$vue.ExtractPropTypes<{
  containerId: {
    type: StringConstructor;
    default: string;
  };
  options: {
    type: PropType<DocSearchOptions>;
    default: () => {};
  };
}>, () => (VNode | null)[], {}, {}, {}, _$vue.ComponentOptionsMixin, _$vue.ComponentOptionsMixin, {}, string, _$vue.PublicProps, Readonly<_$vue.ExtractPropTypes<{
  containerId: {
    type: StringConstructor;
    default: string;
  };
  options: {
    type: PropType<DocSearchOptions>;
    default: () => {};
  };
}>> & Readonly<{}>, {
  options: DocSearchOptions;
  containerId: string;
}, {}, {}, {}, string, _$vue.ComponentProvideOptions, true, {}, any>;
//#endregion
//#region src/client/helpers/docsearch.d.ts
/**
 * DocSearch client locale options
 *
 * DocSearch 客户端多语言选项
 */
type DocSearchClientLocaleOptions = Partial<DocSearchProps>;
/**
 * DocSearch client options
 *
 * DocSearch 客户端选项
 */
interface DocSearchClientOptions extends DocSearchClientLocaleOptions {
  /**
   * Locale options
   *
   * 多语言选项
   */
  locales?: Record<string, DocSearchClientLocaleOptions>;
}
/**
 * Customize DocSearch options
 *
 * 自定义 DocSearch 选项
 *
 * @param options - DocSearch options, support plain object, ref or getter / DocSearch 选项，支持普通对象，Ref 或 Getter
 *
 * @example
 * ```ts
 * import { defineDocSearchConfig } from '@vuepress/plugin-docsearch/client'
 *
 * // Use plain object
 * defineDocSearchConfig({
 *   translations: {
 *     button: {
 *       buttonText: 'Search',
 *     },
 *   },
 * })
 *
 * // Use ref
 * const options = ref({
 *   translations: {
 *     button: {
 *       buttonText: 'Search',
 *     },
 *   },
 * })
 * defineDocSearchConfig(options)
 *
 * // Use getter
 * defineDocSearchConfig(() => ({
 *   translations: {
 *     button: {
 *       buttonText: isDarkMode.value ? 'Search in dark' : 'Search in light',
 *     },
 *   },
 * }))
 * ```
 */
declare const defineDocSearchConfig: (options: MaybeRefOrGetter<DocSearchClientOptions>) => void;
//#endregion
export { DocSearch, DocSearchClientLocaleOptions, DocSearchClientOptions, DocSearchLocaleData, DocSearchLocaleOptions, DocSearchOptions, defineDocSearchConfig };
//# sourceMappingURL=index.d.ts.map